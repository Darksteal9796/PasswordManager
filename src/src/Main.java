import java.io.IOException;

public class Main {
    public static void main(String[] args) throws IOException {
        storeToFile.createfile();
        storeToFile.writefile(takeInputs.takeInput());
    }
}